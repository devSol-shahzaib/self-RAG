Drop your headshot here as `avatar.png` (a square image works best).

  frontend/public/avatar.png

It will appear in the header and next to every answer automatically.
Until you add it, a friendly emoji placeholder is shown instead.
